export type credit_card = {
    number: string;
    cvc: string;
    date: string;
}

export type Jogador = {
    name: string,
    position: string,
    image: string,
}

export type players_array_length = {
    jogadores: number, 
    nabas: number
}